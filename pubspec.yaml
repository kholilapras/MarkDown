name: ppbprak7_unguided
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  # Menambahkan aset gambar untuk proyek
  assets:
    - assets/images/home_jersey.png
    - assets/images/away_jersey.png
    - assets/images/third_jersey.png
    - assets/images/goalkeeper_jersey.png

  # Jika Anda memiliki gambar tambahan, cukup tambahkan di bawah daftar ini